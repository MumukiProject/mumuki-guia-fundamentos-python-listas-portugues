Você tem dúvidas sobre como remover e adicionar elementos? Vamos revisar os seguintes procedimentos:

* `list.append(lista, item)`: adiciona `item` no final da `lista`
* `list.remove(lista, item)`: retira `item` da `lista`
